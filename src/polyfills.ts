// Zoneless: the app runs under provideZonelessChangeDetection() (see
// app.config.ts), so no zone.js patch is loaded in the production bundle.
// This file is kept (and still wired into angular.json's "polyfills" option)
// as the conventional place to add any future browser polyfill.
